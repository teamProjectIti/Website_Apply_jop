@model WebApplication1.Models.category

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>cat</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Name_Cat" class="control-label"></label>
                <input asp-for="Name_Cat" class="form-control" />
                <span asp-validation-for="Name_Cat" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Describations" class="control-label"></label>
                <input asp-for="Describations" class="form-control" />
                <span asp-validation-for="Describations" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="ID" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
